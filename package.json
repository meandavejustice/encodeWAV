{
  "name": "encodeWAV",
  "version": "0.1.0",
  "description": "encode audio buffers into a wav file in a worker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "meandave",
  "license": "MIT",
  "dependencies": {
    "webworkify": "^1.0.0"
  }
}
